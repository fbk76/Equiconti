name: Build APK

on:
  workflow_dispatch:     # pulsante "Run workflow"
  push:
    branches: [ main ]   # opzionale: parte anche ai push su main

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Equiconti   # la tua directory di progetto

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17 (Temurin) + Gradle cache
        uses
