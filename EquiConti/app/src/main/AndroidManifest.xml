<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".EquiApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="EquiConti"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EquiConti">

        <!-- Inizializza il crash logger prima di tutto (anche prima di Application) -->
        <provider
            android:name="com.cz.equiconti.util.CrashInitProvider"
            android:authorities="${applicationId}.crashinit"
            android:exported="false"
            android:initOrder="1" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
