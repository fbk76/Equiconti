<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema dell’app -->
    <style name="Theme.Equiconti" parent="Theme.Material3.DayNight">
        <!-- Colore status bar trasparente su API 21+ -->
        <item name="android:statusBarColor"
              tools:targetApi="21">@android:color/transparent</item>
        <!-- Aggiungi qui eventuali altri item (colori, typography, ecc.) -->
    </style>

</resources>
