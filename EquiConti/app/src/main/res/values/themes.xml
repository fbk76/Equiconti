<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema dell’app: eredita dal tema Material3 DayNight senza ActionBar -->
    <style name="Theme.Equiconti" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- NIENTE windowActionBar / colorPrimaryVariant ecc. (deprecati in M3) -->
        <!-- Esempi di personalizzazione sicura -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Se usi view system con TextInputLayout/Buttons ecc., il pacchetto material fornisce i widget -->
    </style>

</resources>
