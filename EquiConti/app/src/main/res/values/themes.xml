<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema applicazione: Material3 + niente ActionBar -->
    <style name="Theme.Equiconti" parent="Theme.Material3.DayNight">
        <!-- Rimuove title bar / action bar -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

        <!-- (opzionale) Colori dinamici su Android 12+ -->
        <item name="android:forceDarkAllowed">true</item>
    </style>
</resources>
